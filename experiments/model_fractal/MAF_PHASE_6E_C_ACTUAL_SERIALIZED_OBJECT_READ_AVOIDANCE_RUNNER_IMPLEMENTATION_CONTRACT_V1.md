# MAF Phase 6E-C Actual Serialized Object Read Avoidance Runner Implementation Contract V1

**STATUS: PROSPECTIVE RUNNER IMPLEMENTATION CONTRACT — NO PHASE 6E-C RUNNER EXISTS — NO PHASE 6E-C SCIENCE EXECUTED**

## 1. Purpose

This contract defines the exact implementation requirements for the dedicated Phase 6E-C V1 scientific runner.

It is subordinate to the frozen Phase 6E-C prospective preregistration and MUST NOT broaden, reinterpret, or modify the preregistered scientific question.

The runner implementation is authorized only after this contract is independently statically qualified and frozen.

This contract authorizes no Phase 6E-C scientific execution.

## 2. Frozen Phase 6E-C preregistration

Frozen preregistration commit:

`49ca740a14b672d464d6e1f98c411e7ae0b1b947`

Frozen preregistration path:

`experiments/model_fractal/MAF_PHASE_6E_C_ACTUAL_SERIALIZED_OBJECT_READ_AVOIDANCE_PREREGISTRATION_V1.md`

Frozen preregistration SHA256:

`8a143ec7a978d157e2dd700b541cfef6dc4b4781cc2bf582546443534e9c41e6`

Frozen preregistration Git blob:

`1d526c3ba9e9303311d51ddd2f534ab978b5bc08`

The implementation MUST bind this exact preregistration identity before any Phase 6E-C scientific activity.

## 3. Frozen scientific predecessor state

Phase 6E-A is complete and frozen PASS.

Frozen Phase 6E-A result SHA256:

`f86433603e68fc9f1e5c65f7d3ecbd8d9ce9313d252c605dd6e2cada18a91c86`

Frozen Phase 6E-A verdict SHA256:

`a6c9ce2f2207beb252b2c02b47e27878b27e72736dad4275ab9905d40812e334`

Phase 6E-B-S1 is complete, frozen, and PASS.

Frozen Phase 6E-B-S1 result SHA256:

`194857dd4df6baabbee3138f1e6cb3d75988ddd092a4fd877892ab4528cf81a4`

Frozen Phase 6E-B-S1 trace SHA256:

`5fab94f632b8e653b2debe3f5e98f9a19b9688bd9bacb4a578c75537c1b0ca53`

Frozen Phase 6E-B-S1 verdict SHA256:

`6ba5427a18d4bbbb2435456250874372016438367d8c591913432c084c627abf`

The original Phase 6E-B execution remains a spent infrastructure/integrity failure and contributes no scientific recovery result.

Frozen failure incident SHA256:

`1ecf171bee1e6470d5ac48c1482a2166eca05c895313eeb4acbc8e90b64a64ad`

Original Phase 6E-B result path:

`experiments/model_fractal/maf_phase_6e_b_bounded_expansion_recovery_v1.json`

That original result path MUST remain absent permanently.

The original Phase 6E-B runner MUST NOT be imported, called, or executed by Phase 6E-C.

## 4. Exact scientific claim boundary

Phase 6E-C V1 tests only:

`generation-bound serialized MAF object/tensor-payload read avoidance`

The observable claim is an application-level serialized-object read claim at the frozen `maf_segment_reader_v1.read_serialized_object` boundary.

Phase 6E-C V1 does not claim:

- physical disk-sector avoidance;
- operating-system page-cache avoidance;
- storage-controller avoidance;
- filesystem-block avoidance;
- kernel readahead avoidance;
- hardware I/O equivalence;
- dense-runtime replacement;
- arbitrary-prompt generalization;
- full-model execution avoidance;
- end-to-end inference;
- answer generation;
- output fidelity;
- logits parity;
- top-k parity;
- token agreement;
- answer quality;
- latency improvement;
- throughput improvement;
- memory improvement;
- energy improvement;
- MAF-native compute.

Output and quality fidelity remain Phase 6E-D.

Performance remains Phase 6F.

MAF-native compute remains `disabled_unvalidated`.

## 5. Frozen source authorities

Before a Phase 6E-C runner can be frozen, its source MUST bind the exact authorities below.

### Phase 6E entry checkpoint

Path:

`experiments/model_fractal/MAF_PHASE_6E_ENTRY_CHECKPOINT.md`

SHA256:

`1807437ea97e46a6cc687adbbe8c384dabae45a562f1d2aef6bd37e43b4bbe35`

### Phase 6E-A result

Path:

`experiments/model_fractal/maf_phase_6e_a_selective_working_set_sufficiency_v1.json`

SHA256:

`f86433603e68fc9f1e5c65f7d3ecbd8d9ce9313d252c605dd6e2cada18a91c86`

### Phase 6E-A verdict

Path:

`experiments/model_fractal/MAF_PHASE_6E_A_SELECTIVE_WORKING_SET_SUFFICIENCY_V1_VERDICT.md`

SHA256:

`a6c9ce2f2207beb252b2c02b47e27878b27e72736dad4275ab9905d40812e334`

### Phase 6E-B-S1 result

Path:

`experiments/model_fractal/maf_phase_6e_b_s1_bounded_expansion_recovery_v1.json`

SHA256:

`194857dd4df6baabbee3138f1e6cb3d75988ddd092a4fd877892ab4528cf81a4`

### Phase 6E-B-S1 verdict

Path:

`experiments/model_fractal/MAF_PHASE_6E_B_S1_BOUNDED_EXPANSION_RECOVERY_V1_VERDICT.md`

SHA256:

`6ba5427a18d4bbbb2435456250874372016438367d8c591913432c084c627abf`

### Original Phase 6E-B failure incident

Path:

`experiments/model_fractal/MAF_PHASE_6E_B_EXECUTION_FAILURE_INCIDENT_AND_CORRECTIVE_SUCCESSOR_DESIGN_V1.md`

SHA256:

`1ecf171bee1e6470d5ac48c1482a2166eca05c895313eeb4acbc8e90b64a64ad`

### Frozen query population

Path:

`experiments/model_fractal/maf_query_to_pk_selection_validation_v1_queries.json`

SHA256:

`32f4636bd5e8ceee0dca8342335279a6f8e6acafe866a5469f2c23aa2655202a`

### Frozen catalog

Path:

`experiments/model_fractal/maf_query_to_pk_selection_validation_v1_catalog.json`

SHA256:

`c51ef0fabeb10e7c8a091a093aa1934620a69b2026b5a87cb02477b9dd7ca900`

### Frozen selector protocol

Path:

`experiments/model_fractal/MAF_QUERY_TO_PK_SELECTION_V1_PROTOCOL.md`

SHA256:

`b42a0643976c2393871e3b826ba5573b4de981b49603de63230d69cf57be0811`

### Frozen selector implementation

Path:

`experiments/model_fractal/maf_query_to_pk_selection_v1.py`

SHA256:

`e21c05e352fe921b791e8c936425727a98911ce7566989672a96bfd2b9613fa2`

### Frozen capsule protocol

Path:

`experiments/model_fractal/MAF_QUERY_CAPSULE_DATA_MODEL_V1_PROTOCOL.md`

SHA256:

`5255a58c8c6340c251e24ae8543760fcd66a8deb8be1a8356ad5b78ad898edda`

### Frozen capsule implementation

Path:

`experiments/model_fractal/maf_query_capsule_data_model_v1.py`

SHA256:

`f897b97011714f3c84b7350c6bb53e800232be8a5d34e5148a3e50a81952976e`

### Frozen generation construction authority

Path:

`experiments/model_fractal/maf_query_to_pk_selection_validation_v1_generation_construction_authority.json`

SHA256:

`a5e953ae2ab2dd11a9f6dc564ae5ee57cb064d6eea0a8a5517191b4a6efc1a6d`

### Frozen generation manifest

Path:

`results/runtime/maf_query_to_pk_selection_validation_v1_generation/validation_generation.manifest.json`

SHA256:

`28e4baaf07fae450d3c8462ed86fe59c31f0eacdd9e16a8b408994f2a54924c3`

### Frozen reference fixture

Path:

`experiments/model_fractal/maf_phase_6e_a_reference_state_fixture_v1.json`

SHA256:

`870b656e0c3181ec8f7be1537bdeafde10fffd1b947f5372d21cdee4c00a83a0`

### Frozen MAF object implementation

Path:

`experiments/model_fractal/maf_object_v1.py`

SHA256:

`eed6cbd96995412a632883f3c534f9023e7711ea8eb437afe35a48312502f63b`

### Frozen segment reader

Path:

`experiments/model_fractal/maf_segment_reader_v1.py`

SHA256:

`3499cb8e528fe8e9315e3e5656d6aa888c95ca175310b6371e722de409827369`

### Frozen telemetry data model

Path:

`experiments/model_fractal/maf_segment_locality_data_model_v1.py`

SHA256:

`5432f2d74a610f2d0f9181e9af146013bb198a4e837af634993909358fff8ad0`

### Frozen runtime telemetry integration

Path:

`experiments/model_fractal/maf_segment_locality_runtime_telemetry_integration_v1_1.py`

SHA256:

`850c4bae5f6c361c34f504b5af76b9ac6530980d777f8bd1a9003f3b9bb55ae6`

### Frozen object runtime residency implementation

Path:

`experiments/model_fractal/maf_object_runtime_residency_v1.py`

SHA256:

`4b8c0b8f5db9a67b4bcc368b18f159de6a3f2501f0badf0286de1459125d5cf9`

The runner MUST also bind its frozen implementation contract SHA256 after this contract is frozen.

## 6. Historical Phase 6E-A runner authority

For provenance only, the historical Phase 6E-A runner is:

`experiments/model_fractal/maf_phase_6e_a_selective_working_set_sufficiency_v1.py`

Frozen commit:

`7cf66034e3cf66356584cd3f02b1d2c556e31a64`

Exact SHA256:

`72c0eca00013e0b70c4a95c4043f0a11b0b6b3d885166a898014f74b928961d6`

This SHA is exactly 64 hexadecimal characters.

Phase 6E-C MUST NOT execute the Phase 6E-A runner.

## 7. Frozen model and generation identity

Source model PK:

`mafmodel:v1:d670768d29daf84be95501480a777fd1ee5fddda18f4d0a4c8c3953e1b8cc258`

Source generation PK:

`mafgen:v1:45a80a2aa271ce04853003185b6a1227cb309bd1e3e2dc538eea849fac9f5db0`

Frozen source GGUF SHA256:

`507de59046601282ba768a9789900e6ccf60ed93ddf346730b7c68eb0715bc47`

The scientific runner MUST carry the source GGUF SHA256 as provenance.

Source GGUF payload access is prohibited during the Phase 6E-C scientific runner.

The runner MUST NOT open, mmap, seek, read, hash, sample, deserialize, or otherwise access source GGUF payload bytes.

No source-GGUF read may be counted as treatment evidence.

## 8. Frozen population and denominator

The frozen query population is exactly 40 queries:

- 24 `specific_intent`;
- 8 `multi_target_intent`;
- 8 `fallback_control`.

The Phase 6E-C V1 scientific PASS denominator is exactly the 32 frozen non-fallback queries.

The eight fallback queries remain controls and MUST NOT be added to the scientific PASS denominator.

The frozen catalog contains exactly 12 serialized MAF objects.

The frozen generation manifest binds exactly 12 corresponding serialized objects.

The canonical full-reference serialized-object denominator is:

`catalog_serialized_bytes = 312326668`

This denominator is the sum of the exact generation-manifest serialized object lengths for the frozen 12-object catalog.

It is not a source-GGUF byte denominator.

It is not a dense-model byte denominator.

It is not a hardware-I/O byte denominator.

## 9. Scientific treatment definition

For each of the 32 non-fallback queries, the treatment is the deterministic non-oracle query-derived working-set route produced by the frozen selector under the preregistered bounded candidate schedule:

`1 → 2 → 3 → 4`

Round 0:

- candidate budget: `1`
- selection-config SHA256:
  `2dd2925a3d9f63197deff6dc98f951c5177dc63dc08818b6492228dbac7d5576`

Round 1:

- candidate budget: `2`
- selection-config SHA256:
  `57f751a3a019bccd58c5c32b4f5ecaf78283f49cc146896d61492ce31dfdd1c6`

Round 2:

- candidate budget: `3`
- selection-config SHA256:
  `67dd6e5589cb16374e949b3ed633c3364f0ccb1d8c2b0a9ca1c6eaa2d2ceed56`

Round 3:

- candidate budget: `4`
- selection-config SHA256:
  `0caeaeaafdd870e6b02ccc8b4450576883d4ef5234ba689c472e3b14d9dbe120`

The treatment read set for one query is the deterministic unique cumulative route after the complete frozen 1→2→3→4 selector schedule.

The runner MUST NOT replace this treatment set with:

- the reference required-object set;
- Phase 6E-A selected PKs;
- Phase 6E-B-S1 required PKs;
- a hand-authored PK set;
- an oracle-complete set;
- all 12 catalog PKs;
- a cache-derived route;
- a relationship-derived route.

## 10. Full-reference access denominator

The full-reference comparison is a counterfactual application-level access set consisting of all 12 frozen serialized objects.

The runner MUST compute the full-reference byte denominator from frozen manifest metadata only.

The runner MUST NOT physically read all 12 serialized objects merely to instantiate the denominator.

The counterfactual denominator for each scientific query is:

- object count: 12;
- serialized bytes: `312326668`.

The aggregate full-reference denominator for 32 scientific queries is exactly:

`32 * 312326668`

The runner MAY emit that product as an integer result field.

The denominator is mathematical and manifest-bound, not measured by forcing prohibited full-reference payload reads.

## 11. Exact scientific read boundary

The only serialized-object payload-carrying function authorized for the Phase 6E-C scientific observation window is:

`maf_segment_reader_v1.read_serialized_object`

A treatment object is scientifically counted as touched only if the runner actually invokes this frozen function for that object in the observation window and the invocation completes successfully with the exact expected serialized object bytes.

A selected PK is not automatically a touched PK.

A planned PK is not automatically a touched PK.

A capsule-selected PK is not automatically a touched PK.

A PK becomes touched only through a successful observed serialized-object read event.

## 12. `touched_pks` causal rule

The capsule `touched_pks` field is a scientific observation surface.

It MUST be populated from successful observed read events.

It MUST NOT be populated merely by copying `selected_object_pks`.

Before the scientific read window begins, every treatment capsule used for observation MUST have no scientifically asserted touched PKs.

After each successful serialized-object read, the runner may append exactly that successfully read object PK to the observed touched sequence.

If a read fails, the PK MUST NOT be recorded as touched.

The final scientific `touched_pks` order MUST equal the successful `read_serialized_object` event order exactly.

For every query:

`touched_pks ⊆ selected_object_pks`

For a successful complete treatment read:

`touched_pks == selected_object_pks`

The runner MUST separately retain:

- selected PKs;
- planned read PKs;
- successfully touched PKs;
- untouched catalog PKs.

## 13. Observation-window contamination barrier

The Phase 6E-C scientific observation window begins immediately before the first treatment `read_serialized_object` invocation and ends immediately after the final treatment `read_serialized_object` invocation for the frozen 32-query scientific population.

During that window the runner MUST NOT call:

`maf_object_v1.inspect_object`

The scientific runner MUST NOT call `inspect_object` during its scientific read-observation window.

The runner also MUST NOT call any helper that independently opens and scans the same serialized object payload for scientific evidence during the observation window.

The following are prohibited during the scientific observation window:

- object-file SHA scans independent of `read_serialized_object`;
- segment-wide SHA scans;
- source-GGUF reads;
- source-GGUF hashing;
- direct object `open(..., "rb")` payload scans;
- direct segment `open(..., "rb")` payload scans;
- direct `Path.read_bytes()` on serialized objects;
- direct `os.pread()` of treatment object payloads;
- dense materialization;
- inference;
- Route Cache lookup;
- relationship expansion.

The runner may consume query, catalog, manifest, generation-authority, and frozen predecessor-result metadata outside the observation window subject to the anti-oracle requirements below.

## 14. Metadata-only reads

Metadata-only reads MUST be distinguished from serialized-object payload-carrying reads.

The following frozen files may be read as metadata/provenance outside the scientific observation window:

- query fixture;
- catalog;
- generation construction authority;
- generation manifest;
- Phase 6E-A result;
- Phase 6E-A verdict;
- Phase 6E-B-S1 result;
- Phase 6E-B-S1 verdict;
- failure incident;
- Phase 6E entry checkpoint;
- selector/capsule protocols and implementations for identity verification;
- telemetry authorities for identity verification;
- preregistration and runner-contract bytes for identity verification.

Metadata-only reads MUST NOT increment:

- scientific serialized-object read count;
- scientific treatment byte count;
- `touched_pks`;
- `BYTES_READ` serialized-object treatment telemetry.

The result MUST expose that metadata reads are excluded from the serialized-object treatment-byte claim.

## 15. Serialized-object read accounting

For every successful treatment read event, the runner MUST record at minimum:

- query ID;
- query PK;
- query class;
- round-complete treatment plan identity;
- object PK;
- generation PK;
- segment identifier or generation-bound segment path identity;
- offset;
- exact serialized span length;
- expected serialized-object SHA256;
- observed returned-byte length;
- observed returned-byte SHA256;
- event ordinal;
- success/failure state.

The scientific byte charge for one successful serialized-object read is exactly the generation-bound serialized span length supplied to the frozen segment reader.

The runner MUST require:

`observed_returned_byte_length == serialized_span_length`

and:

`sha256(returned_serialized_bytes) == expected_serialized_object_sha256`

before the event may count as a successful touch.

No inferred or estimated byte count is allowed.

## 16. Duplicate-read prohibition

For a single query, the runner MUST read each unique treatment object at most once.

The planned read sequence MUST be deduplicated by object PK while preserving first occurrence in the frozen cumulative route.

A duplicate scientific read of the same object for the same query is an infrastructure/integrity failure.

Cross-query reads remain separate scientific events because each query is an independent treatment observation.

## 17. Deterministic read-plan snapshot

Before any hidden reference semantics are parsed, the runner MUST complete the non-oracle treatment planning for the complete frozen query population.

At minimum it MUST freeze one canonical read-plan snapshot containing, for all 40 queries:

- query order;
- query ID;
- query PK;
- query class;
- query signature;
- each of the four selector rounds;
- candidate budget per round;
- exact selection-config SHA256 per round;
- selected PKs per round;
- deterministic additional PKs per round;
- cumulative route per round;
- final unique planned treatment read order;
- capsule identity required by the preregistered design.

The snapshot MUST NOT contain:

- required-object PKs from the hidden reference;
- reference payload SHA256 values;
- expected payload SHA256 values derived from hidden reference;
- recovery verdicts copied from Phase 6E-B-S1;
- Phase 6E-A selected PKs as selector inputs;
- avoidance verdicts;
- pass/fail classifications.

The complete snapshot MUST be canonicalized and SHA256-hashed before hidden reference semantics are parsed.

Recommended trace schema:

`openmind.maf_phase_6e_c_serialized_object_read_plan_snapshot.v1`

The result MUST expose the exact read-plan snapshot SHA256.

## 18. Trace-before-reference anti-oracle barrier

The execution order MUST be fail-closed.

The runner MUST:

1. verify pre-execution repository and frozen-authority state;
2. import only authorized frozen implementation modules;
3. load non-oracle query/catalog/generation metadata;
4. construct the exact four round configs;
5. execute the frozen selector schedule for all 40 queries;
6. construct the complete planned read-set snapshot;
7. canonicalize and hash the immutable complete read-plan snapshot;
8. only then parse the frozen reference fixture for scientific evaluation;
9. derive required-object evaluation state without modifying the frozen plan;
10. execute exactly the already-frozen treatment reads for the 32 non-fallback queries;
11. derive event-sourced touched PKs and byte counts;
12. evaluate correctness-preservation and avoidance conditions;
13. construct one result;
14. publish the result immutably.

No reference-informed change to the planned read set is permitted.

## 19. Reference use after plan freeze

The frozen reference fixture may be semantically parsed only after the complete treatment read plan is frozen and hashed.

Reference semantics may be used only to evaluate:

- required object PKs for each non-fallback query;
- whether every required PK was included in the already-frozen planned treatment read set;
- whether every required PK was actually touched successfully;
- which catalog objects were unnecessary for that frozen query;
- whether unnecessary objects remained untouched.

Reference semantics MUST NOT be used to choose, reorder, add, remove, retry, or rescue treatment reads.

## 20. Correctness-preservation condition

Phase 6E-C tests avoidance alone and MUST preserve the correctness boundary already established by Phase 6E-A and Phase 6E-B-S1.

For each of the 32 non-fallback scientific queries, the Phase 6E-C runner MUST require:

- every reference-required object PK is present in the frozen planned treatment set;
- every reference-required object PK is present in the successful touched set;
- every successful touched serialized object exactly matches the frozen generation-manifest serialized-object SHA256;
- no required object is missing after treatment reads.

If any required object is absent from the planned treatment set or successful touched set, Phase 6E-C is not allowed to claim avoidance success for that query.

The runner MUST NOT add the missing required object after observing the reference.

## 21. Untouched-object condition

For each scientific query:

`untouched_catalog_pks = catalog_pks - touched_pks`

The order MUST be deterministic catalog order.

Every catalog object not present in the frozen treatment set MUST remain absent from the successful read event sequence.

The runner MUST prove this by exact set and sequence accounting, not by assumption.

For each scientific query the result MUST expose:

- `catalog_object_count`;
- `selected_object_count`;
- `touched_object_count`;
- `untouched_object_count`;
- `catalog_serialized_bytes`;
- `treatment_serialized_bytes`;
- `avoided_serialized_bytes`;
- `avoided_serialized_fraction`.

The byte arithmetic MUST satisfy:

`avoided_serialized_bytes = catalog_serialized_bytes - treatment_serialized_bytes`

and:

`treatment_serialized_bytes + avoided_serialized_bytes = catalog_serialized_bytes`

The fraction is descriptive only and is not a performance metric.

## 22. PASS / FAIL scientific rule

Phase 6E-C V1 is `PASS` if and only if all 32 frozen non-fallback scientific queries satisfy all of the following:

1. the treatment read plan was frozen before reference semantics were parsed;
2. the treatment plan came only from the frozen non-oracle selector under the exact 1→2→3→4 schedule;
3. successful touched PKs exactly equal the frozen planned treatment read PKs;
4. all required PKs are included in the touched PKs;
5. every touched serialized object matches its frozen generation-manifest serialized-object identity;
6. no nonselected catalog object was read through `read_serialized_object`;
7. `treatment_serialized_bytes < catalog_serialized_bytes`;
8. `untouched_object_count > 0`;
9. `avoided_serialized_bytes > 0`;
10. no prohibited source-GGUF, Route Cache, relationship-expansion, `inspect_object`, dense-materialization, or inference access occurred in the scientific observation window.

If one or more of the 32 scientific queries violates any scientific PASS condition under an otherwise valid execution, Phase 6E-C V1 is `FAIL`.

Infrastructure/integrity failures MUST remain distinct from scientific FAIL.

## 23. Fallback controls

The eight `fallback_control` queries MUST remain identifiable as controls.

They are excluded from the 32-query PASS denominator.

The runner MUST NOT use fallback controls to inflate avoided-object counts or avoided-byte fractions.

The runner MAY preserve their non-oracle selector-plan trace for population completeness.

The scientific result MUST clearly distinguish fallback control records from the 32 treatment records.

No fallback record may be silently reclassified as a scientific treatment query.

## 24. Telemetry requirements

The frozen telemetry surfaces include:

- `MATERIALIZATION`;
- `BYTES_READ`.

Phase 6E-C runner construction MUST statically prove that the chosen telemetry API is deterministic and compatible with the frozen telemetry data model before the runner is frozen.

Scientific telemetry MUST be observational only.

Telemetry MUST NOT:

- select objects;
- alter selector ranking;
- alter capsule routes;
- trigger expansion;
- add reads;
- retry reads;
- suppress reads;
- change scientific classifications.

Each successful serialized-object treatment read MUST produce deterministic read accounting equivalent to one `BYTES_READ` event charged with the exact serialized span length.

A materialization/access event may be recorded only as defined by the frozen telemetry API and MUST NOT replace the actual `read_serialized_object` event as the causal touch boundary.

If telemetry faults, loses events, double-counts events, or disagrees with independently accumulated treatment byte arithmetic, scientific telemetry export is invalid and the runner MUST fail closed before result publication.

## 25. Independent event ledger

The runner MUST maintain an independent deterministic event ledger in addition to any telemetry accumulator.

This ledger is the primary exact scientific accounting surface.

For each read call the ledger MUST distinguish:

- planned;
- attempted;
- succeeded;
- failed.

Only succeeded events contribute to:

- touched PKs;
- treatment serialized bytes;
- avoided byte arithmetic.

The telemetry aggregate and event ledger MUST reconcile exactly before result publication.

## 26. Segment-reader call discipline

The frozen `read_serialized_object` function MUST remain unmodified.

The Phase 6E-C runner MUST NOT monkeypatch its internal file-read semantics.

Instrumentation may wrap the call boundary only to record deterministic before/after state.

The wrapper MUST NOT alter:

- entry identity;
- generation identity;
- path;
- offset;
- length;
- expected serialized SHA256;
- returned bytes;
- exception behavior.

If the exact entry type required by the frozen segment reader cannot be constructed from already frozen generation-bound metadata and the segment-reader dependency surface without changing scientific semantics, runner construction MUST stop before science and return to a prospective implementation-design gate.

No scientific execution may be used to discover or debug that API.

## 27. Runtime residency boundary

The frozen object runtime residency implementation may be imported only if required by the preregistered instrumentation surface and only after exact static qualification.

Dense materialization MUST remain disabled.

No configured dense materializer may be invoked.

The runner MUST NOT use runtime residency to create an alternate payload read path that bypasses `read_serialized_object`.

Any runtime-residency state included in the result is ancillary implementation evidence, not a replacement for the exact segment-reader read ledger.

## 28. Route Cache boundary

Query Route Cache is disabled for Phase 6E-C V1 unless the frozen preregistration explicitly requires otherwise.

The Phase 6E-C scientific runner MUST NOT import, call, populate, read, or invalidate the Query Route Cache for treatment selection or read planning.

Cached routes MUST NOT replace fresh frozen-selector invocations.

## 29. Relationship expansion boundary

Relationship-based expansion is disabled for Phase 6E-C V1.

`selected_relationship_pks` MUST remain empty where the frozen capsule contract requires that field.

No relationship-derived PK may enter the treatment read plan.

## 30. Spent runner prohibitions

The original frozen Phase 6E-B runner MUST NOT be imported, called, or executed.

The spent Q2 V1.4 runner:

`maf_query_to_pk_selection_validation_v1_4.py`

MUST NOT be imported, called, or executed.

Its path or historical SHA may appear only in static provenance text when necessary.

The Phase 6E-A runner MUST NOT be executed.

The Phase 6E-B-S1 runner MUST NOT be executed again.

Frozen predecessor result JSON may be read as metadata authority only when the preregistration permits it.

## 31. Module import boundary

The dedicated Phase 6E-C runner MUST keep dynamic scientific imports minimal and explicit.

At minimum, the intended scientific implementation modules are:

- frozen query-to-PK selector;
- frozen query capsule data model;
- frozen segment reader;
- frozen telemetry data model or frozen telemetry integration only if required by the final runner design.

The runner MUST NOT import unrelated validation runners as scientific implementation dependencies.

No network client library is required or authorized.

## 32. No network

Phase 6E-C V1 is fully local.

The runner MUST NOT:

- access the network;
- resolve remote URLs;
- download models;
- upload artifacts;
- push Git;
- access remote APIs.

Network activity is an infrastructure/integrity failure.

## 33. Canonical JSON discipline

The Phase 6E-C runner MUST use one explicitly defined canonical JSON convention for its own trace and result artifacts.

Recommended convention:

```python
json.dumps(
    value,
    sort_keys=True,
    ensure_ascii=False,
    separators=(",", ":"),
).encode("utf-8")
```

with no terminal LF.

The runner MUST NOT change canonicalization after scientific target observation.

The reference-fixture serialization convention remains a separate frozen historical issue and MUST be handled exactly as required by the frozen Phase 6E-C preregistration.

## 34. Result schema and path

Recommended result schema:

`openmind.maf_phase_6e_c_actual_serialized_object_read_avoidance.v1`

Recommended result path:

`experiments/model_fractal/maf_phase_6e_c_actual_serialized_object_read_avoidance_v1.json`

The original Phase 6E-B result path MUST remain absent permanently.

The runner MUST fail closed if the Phase 6E-C result path already exists before the authoritative execution.

## 35. Minimum result fields

The Phase 6E-C result MUST bind and expose at minimum:

- result schema;
- phase label `6E-C`;
- frozen Phase 6E-C preregistration SHA256;
- frozen Phase 6E-C runner implementation contract SHA256;
- frozen runner source SHA256;
- frozen runner commit identity supplied by the execution wrapper;
- Phase 6E-A result SHA256;
- Phase 6E-A verdict SHA256;
- Phase 6E-B-S1 result SHA256;
- Phase 6E-B-S1 verdict SHA256;
- Phase 6E-B-S1 trace SHA256;
- failure incident SHA256;
- query fixture SHA256;
- catalog SHA256;
- selector protocol SHA256;
- selector implementation SHA256;
- capsule protocol SHA256;
- capsule implementation SHA256;
- generation authority SHA256;
- generation manifest SHA256;
- reference fixture SHA256;
- segment-reader SHA256;
- telemetry authority SHA256 values used;
- source model PK;
- source generation PK;
- source GGUF SHA256 as provenance only;
- source-GGUF payload-access state;
- read-plan snapshot schema;
- read-plan snapshot SHA256;
- frozen round config SHA256 values;
- query count;
- scientific denominator count;
- fallback control count;
- catalog object count;
- `catalog_serialized_bytes`;
- aggregate full-reference serialized bytes;
- aggregate treatment serialized bytes;
- aggregate avoided serialized bytes;
- aggregate avoided serialized fraction;
- total successful serialized-object read events;
- total failed serialized-object read events;
- telemetry/event-ledger reconciliation state;
- per-query records;
- Phase 6E-C scientific verdict;
- scientific boundary booleans.

## 36. Minimum per-query result record

Each of the 32 scientific query records MUST include at minimum:

- query ID;
- query PK;
- query class;
- query signature;
- final planned treatment PKs;
- successful touched PKs;
- untouched catalog PKs;
- required PKs revealed only after the plan snapshot was frozen;
- required-PK coverage state;
- read-event records;
- catalog object count;
- treatment object count;
- untouched object count;
- catalog serialized bytes;
- treatment serialized bytes;
- avoided serialized bytes;
- avoided serialized fraction;
- serialized-object identity verification state;
- prohibited-read state;
- query-level PASS/FAIL.

Fallback records MUST be marked `CONTROL` and kept outside the scientific denominator.

## 37. Scientific boundary fields

The result MUST make at least the following boundary states explicit:

- `application_level_serialized_object_read_claimed = true`;
- `hardware_physical_io_avoidance_claimed = false`;
- `source_gguf_payload_accessed = false`;
- `full_model_avoidance_claimed = false`;
- `dense_materialization_executed = false`;
- `inference_executed = false`;
- `output_fidelity_claimed = false`;
- `answer_quality_claimed = false`;
- `performance_claimed = false`;
- `maf_native_compute = "disabled_unvalidated"`;
- `route_cache_used = false`;
- `relationship_expansion_used = false`;
- `reference_informed_read_plan = false`;
- `inspect_object_used_in_observation_window = false`;
- `touched_pks_event_sourced = true`;
- `metadata_reads_excluded_from_treatment_bytes = true`;
- `counterfactual_full_reference_physically_read = false`.

## 38. Immutable result publication

The result MUST be published once using exclusive creation semantics equivalent to:

`os.O_WRONLY | os.O_CREAT | os.O_EXCL`

The runner MUST:

1. create the result only after all scientific and integrity checks complete;
2. write exact canonical bytes;
3. flush;
4. fsync;
5. close;
6. reread;
7. verify exact byte equality;
8. verify result SHA256;
9. preserve a successfully created result even if a later reporting-only check fails.

The runner MUST NOT overwrite an existing result.

The runner MUST NOT silently delete or rewrite a successfully published scientific result.

## 39. Infrastructure/integrity failures

Infrastructure/integrity failures include, at minimum:

- wrong branch;
- wrong frozen runner commit binding;
- tracked worktree dirty;
- index nonempty;
- frozen authority SHA mismatch;
- malformed query/catalog/manifest state;
- source generation mismatch;
- unexpected source-GGUF access;
- read-plan mutation after reference parse;
- non-monotonic selector route;
- duplicate per-query object reads;
- observed bytes not matching entry length;
- observed serialized-object SHA mismatch;
- touched PK not causally backed by a successful read event;
- selected/touched ordering mismatch;
- telemetry/event-ledger disagreement;
- forbidden `inspect_object` invocation in the observation window;
- Route Cache use;
- relationship expansion use;
- dense materialization;
- network activity;
- result path already present;
- immutable result publication failure.

An infrastructure/integrity failure is not automatically a scientific FAIL.

If it occurs before valid result publication, the one-shot execution slot is spent and no retry is authorized under the same frozen runner identity.

## 40. Negative-result preservation

If one or more of the 32 scientific treatment queries fails the preregistered avoidance PASS rule under an otherwise valid execution, the scientific result is `FAIL`.

The runner and later workflow MUST preserve that negative result.

They MUST NOT:

- widen the read budget;
- add hidden required PKs after reference observation;
- change the selector;
- change round ordering;
- alter the denominator;
- remove failed queries;
- reinterpret touched PKs;
- change byte arithmetic;
- rerun under the same frozen identity to seek PASS.

## 41. One authoritative execution slot

Phase 6E-C V1 receives exactly one authoritative scientific execution slot.

That slot is authorized only after:

1. Phase 6E-C preregistration is frozen;
2. this runner implementation contract is frozen;
3. a dedicated Phase 6E-C runner source is created;
4. the runner passes syntax and static qualification;
5. the runner is frozen in its own exact Git commit;
6. a separate read-only authoritative execution preflight passes.

The execution wrapper MUST bind the exact frozen runner commit into the child process through an explicit environment variable chosen by the runner contract implementation.

The runner MUST verify that supplied frozen HEAD against `git rev-parse HEAD` before scientific module import or selector execution.

The authoritative wrapper MUST invoke the frozen Phase 6E-C runner exactly once.

No automatic retry is permitted.

## 42. Runner pre-execution repository gate

Before scientific execution, the dedicated runner MUST fail closed unless:

- branch is exactly `labs/multidimensional-maf`;
- HEAD equals the execution-wrapper-supplied frozen runner commit;
- tracked worktree is clean;
- index is empty;
- canonical untracked baseline matches the frozen checkpoint except for explicitly authorized external one-shot sentinel state outside the repository;
- Phase 6E-C result path is absent;
- original Phase 6E-B result path remains absent;
- all frozen authority files match exact expected SHA256;
- no unauthorized candidate/result residue exists.

The runner MUST NOT use Git mutation to repair a failed preflight.

## 43. Static runner qualification requirements

Before runner freeze, static qualification MUST prove at minimum:

- Python syntax and compilation;
- exact preregistration SHA binding;
- exact runner-contract SHA binding;
- exact 20 frozen authority bindings;
- exact historical Phase 6E-A runner SHA shape and identity if referenced;
- exact 40-query and class-count invariant;
- exact 12-object catalog invariant;
- exact `catalog_serialized_bytes = 312326668`;
- exact four-round 1→2→3→4 schedule;
- exact four selection-config SHA256 values;
- use of `read_serialized_object`;
- no scientific `inspect_object` call in the observation window;
- touched PKs derive from successful read events;
- read plan is frozen before reference semantic parse;
- source GGUF is not opened/read/hashed by the scientific runner;
- Route Cache is not imported or used;
- relationship expansion is disabled;
- spent Q2 V1.4 is not imported or used;
- original Phase 6E-B runner is not imported or used;
- Phase 6E-A runner is not executed;
- Phase 6E-B-S1 runner is not executed;
- result uses exclusive creation;
- successful result preservation;
- no network dependency.

Static qualification MUST NOT execute selector science or serialized-object treatment reads.

## 44. Independent result qualification

After the single authoritative execution, a separate independent qualifier MUST verify the result without executing the frozen scientific runner.

The qualifier SHOULD reconstruct, from frozen metadata and result evidence:

- result canonical bytes and SHA256;
- read-plan snapshot SHA256;
- 40-query order;
- 32-query scientific denominator;
- 8 fallback controls;
- four-round schedule;
- planned PK sequence per query;
- successful touched PK sequence;
- untouched catalog PK sequence;
- exact object lengths;
- exact byte arithmetic;
- serialized-object returned SHA evidence;
- aggregate treatment bytes;
- aggregate avoided bytes;
- PASS/FAIL arithmetic;
- prohibited-access boundary fields.

The qualifier MUST NOT rerun the scientific treatment.

## 45. Claim wording after PASS

A valid Phase 6E-C PASS may establish only a narrow claim equivalent to:

> For the frozen 32-query non-fallback population and frozen generation, the preregistered non-oracle query-derived treatment read sets caused successful application-level reads of only their selected generation-bound serialized MAF objects, left at least one catalog object unread for every scientific query, preserved required-object coverage, and read fewer serialized object bytes than the 12-object full-reference counterfactual for every scientific query.

It MUST NOT be rewritten as:

- the model avoided all unnecessary physical I/O;
- MAF is faster;
- MAF uses less memory;
- MAF preserves output quality;
- MAF replaces dense inference;
- MAF avoids all unselected model tensors globally;
- MAF-native compute is validated.

Those questions remain outside Phase 6E-C V1.

## 46. Construction authorization boundary

Freezing this contract authorizes only:

- creation of the dedicated Phase 6E-C runner source;
- static syntax checking;
- static AST/source qualification;
- exact hash/blob qualification;
- freeze of that exact runner source after qualification.

It does not authorize:

- selector scientific execution;
- capsule construction against the live Phase 6E-C treatment population for science;
- serialized-object treatment reads;
- hidden reference semantic parsing for Phase 6E-C science;
- source-GGUF payload access;
- dense materialization;
- inference;
- result creation;
- Phase 6E-D claims;
- Phase 6F claims;
- MAF-native compute;
- network publication.

## 47. Required implementation order

After this contract is frozen:

1. create the dedicated Phase 6E-C runner source;
2. statically qualify exact authority bindings and syntax;
3. statically qualify the non-oracle four-round read-plan construction;
4. statically qualify the trace-before-reference barrier;
5. statically qualify `read_serialized_object` as the sole treatment read boundary;
6. statically qualify event-sourced `touched_pks`;
7. statically qualify byte accounting and telemetry reconciliation;
8. statically qualify source-GGUF and `inspect_object` prohibitions;
9. statically qualify immutable result publication;
10. freeze the exact runner source;
11. perform a separate read-only authoritative execution preflight;
12. consume exactly one authoritative Phase 6E-C execution slot;
13. independently qualify the result;
14. freeze the qualified result;
15. create and freeze a narrow Phase 6E-C verdict document.

No step may be skipped because a favorable scientific result is expected.

## 48. Current state at contract-candidate creation time

At candidate creation time:

- branch: `labs/multidimensional-maf`;
- current frozen preregistration HEAD: `49ca740a14b672d464d6e1f98c411e7ae0b1b947`;
- Phase 6E-A: complete / frozen / PASS;
- Phase 6E-B-S1: complete / frozen / PASS;
- original Phase 6E-B execution: spent infrastructure/integrity failure;
- original Phase 6E-B result: absent permanently;
- Phase 6E-C preregistration: frozen;
- Phase 6E-C runner implementation contract: candidate only;
- Phase 6E-C runner: absent;
- Phase 6E-C result: absent;
- Phase 6E-C scientific execution: not authorized;
- Phase 6E-D: later;
- Phase 6F: later;
- MAF-native compute: disabled / unvalidated.

## 49. Next gate

After this implementation-contract candidate passes independent static qualification, the next authorized mutation is:

`FREEZE PHASE 6E-C RUNNER IMPLEMENTATION CONTRACT`

Only after that freeze may Phase 6E-C runner source bytes be created.

## 50. Final contract statement

The dedicated Phase 6E-C runner must freeze a complete non-oracle 1→2→3→4 treatment read plan before hidden reference semantics are parsed, then execute exactly that immutable plan through the frozen `read_serialized_object` boundary, derive `touched_pks` only from successful read events, reconcile exact serialized-object byte accounting against the 12-object `312326668`-byte counterfactual denominator, preserve required-object coverage, and classify only application-level serialized-object read avoidance.

No Phase 6E-C scientific result is authorized or observed by this implementation contract.
