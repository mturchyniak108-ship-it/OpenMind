# Reproducibility

Record the exact Git commit, model identifier and hash where appropriate, hardware, software versions, command, parameters, input data, outputs, and validation results.

When deterministic execution is possible, use it. Otherwise record sources of nondeterminism and acceptable variation.
