# Security

Principles: least privilege, local-first operation, explicit network access, dependency hygiene, no secrets in Git, and reproducible builds where practical.

Treat external documents and scraped content as untrusted input.

Never commit API keys, passwords, private keys, access tokens, or credentials.
