# Experiment Workflow

```text
Question -> Baseline -> Experiment -> Control -> Result -> Holdout -> Validation -> Provenance
```

Every experiment should record objective, inputs, model, environment, command, parameters, output, interpretation, and limitations.

The experiment dependency graph should remain acyclic.
