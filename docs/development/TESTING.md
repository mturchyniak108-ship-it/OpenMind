# Testing

Minimum Python validation:

```bash
python -m compileall -q .
git diff --check
```

Use unit, integration, regression, benchmark, and holdout testing as appropriate.

A passing test proves the test passed; it does not prove a research hypothesis.
